# Copy this file to publish.config.ps1 and set your local deploy path.
# publish.config.ps1 is gitignored — never commit it.

# Directory where the built binary will be deployed.
# Subdirectories backups/ and logs/ are created automatically.

# Windows example:
# $DeployDir = "C:\Programs\smart-todo"

# macOS example:
# $DeployDir = "$HOME/Applications/smart-todo"

# Linux example:
# $DeployDir = "$HOME/.local/bin/smart-todo"

$DeployDir = "C:\Programs\smart-todo"
